# Placitum counter

[English](README.md) · Русский

Поведенческий инспектор Placitum. Инспекторы фазы запроса оценивают намерение — что клиент прислал.
Счётчик оценивает результат — что клиент **унёс**: скрейпер, перебирающий каталог обычными
запросами, для сигнатур неотличим от покупателя, и отличает их только сумма.

Один subject, три фазы:

```
ответ   ──►  measure: ответ превращается в начисления бакетов (сколько объектов, сколько килобайт)
запрос  ──►  judge:   уровни бакетов против порогов → score | deny | действия соседям
кадры   ──►  обе работы на каждом кадре WebSocket, ось conn — до конца соединения
```

Бакеты живут во внутреннем Redis (GCRA, та же арифметика, что у капчи). Счётчики объявляются
**на инспектор**, а не на профиль: общая секция `profiles/_shared/counters.yaml`, пороги профилей —
проценты заполнения.

Незнание — `verdict: error`: повреждённое сообщение, чужая версия, неизвестный профиль, молчащий Redis
бакетов. Измерения не было, и выбирать между пропуском и блокировкой счётчик не вправе — это решает
маршрут.

## Оси

| Ось | Субъект |
| --- | --- |
| `ip` | адрес клиента |
| `asn_net`, `asn_router` | анонс и автономная система адреса (нужен `geo`) |
| `sess` | ключ сессии, по умолчанию кука идентификатора пропуска капчи `waf_cid` |
| `user` | ключ клиента из куки или заголовка либо пользователь от проверки входа (`session:user`, `session:sid`) |
| `conn` | соединение WebSocket, пока оно открыто |

## Сборка

```sh
docker build -f deploy/Dockerfile -t placitum/counter .
```

В образе есть проба: `HEALTHCHECK` шлёт обычное сообщение по шине с профилем `_probe` и ждёт
`deny`, то есть проверяет разом шину, подписку, профили и воркеров.

Что нужно рядом и все настройки — в [INSTALL.ru.md](INSTALL.ru.md).

## Устройство

```
cmd/inspector/     шина, waf.req.counter, все фазы; ключи осей
cmd/probe/         проверка здоровья: профиль _probe с порогом 0
internal/buckets/  бакеты GCRA
internal/measure/  правила фазы ответа: предикат → источник → начисление
internal/decide/   оценка по уровням, сигналы соседей — чистые функции
internal/config/   окружение, counters.yaml, профили и горячая замена
internal/desired/  поколение из KV (policy/counter)
profiles/          _shared/counters.yaml, default, _probe
```

Общий код процессов — сигнал активности, снимок машины, счётчики потока, уровни журнала, клиент справочника сетей —
берётся из [`placitum-shared`](https://github.com/exemt/placitum-shared).

## Лицензия

[Placitum License Agreement](LICENSE.md). Перевод на русский лежит в [LICENSE.ru.md](LICENSE.ru.md),
юридическую силу имеет английский текст.
